apiVersion: v1
kind: Service
metadata:
  name: tidu-kf-webapp
  namespace: kubeflow
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/component: tidu-kf-webapp
  type: ClusterIP
